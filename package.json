{
  "name": "yotton-exception-sanitizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && node scripts/verify-site.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "indexnow": "node scripts/submit-indexnow.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
